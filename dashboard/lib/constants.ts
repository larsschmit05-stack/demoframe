// Demo limits
export const FREE_DEMO_LIMIT = 3;
export const MAX_SCREEN_SIZE_BYTES = 15 * 1024 * 1024; // 15MB per screen
export const MAX_SCREENS_PER_DEMO = 20;
export const STORAGE_BUCKET = 'recordings'; // keeping existing bucket name

// Legacy (still used by old recording routes during migration)
export const FREE_RECORDING_LIMIT = 3;
export const MAX_UPLOAD_SIZE_BYTES = 5 * 1024 * 1024;
